# Tailscale node IP the admin UI (and tailnet proxy port) bind to.
TAILSCALE_IP=100.x.x.x

# Admin UI HTTP Basic auth. Strongly recommended even on a tailnet.
# If ADMIN_PASSWORD is empty, admin auth is DISABLED (a warning is logged).
ADMIN_USER=admin
ADMIN_PASSWORD=change-me
